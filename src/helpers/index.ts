import AudioHelper from './Audio';
import DomHelper from './Dom';
import MathHelper from './Math';
import RenderHelper from './Render';

export namespace Helpers {
    export let Audio = AudioHelper;
    export let Dom = DomHelper;
    export let Math = MathHelper;
    export let Render = RenderHelper;
}

export default Helpers;